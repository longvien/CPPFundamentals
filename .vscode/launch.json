{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (Mac LLDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/main",
      "cwd": "${fileDirname}",
      "stopAtEntry": false,
      "externalConsole": true,
      "MIMode": "lldb",
      "preLaunchTask": "build"
    }
  ]
}