#include <bits/stdc++.h>

using namespace std;
int main() {
    vector<int> v;
    for (int i = 0; i < 5; i ++) {
        v.push_back(i);
    }
    for (int num : v) {
        cout << num << " ";
    };

    vector<double> a = {1.5, 2.5, 3.5};
    for (auto num : a) {
        cout << num << " ";
    }


    return 0;

    




}